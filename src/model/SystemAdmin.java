package model;

/**
 * System administrator user who manages reports and passwords.
 */
public class SystemAdmin extends User {

    public SystemAdmin(String id, String name, String username, String password) {
        super(id, name, username, password);
    }
}
